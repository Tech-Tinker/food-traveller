import React from 'react';

function MapDisplay({ map, marker }) {
  return (
    <div id="map" style={{ width: '100%', height: '500px' }}>
      {/* El mapa se renderizará dentro de este div */}
    </div>
  );
}

export default MapDisplay;
